#!/usr/bin/env bash


while getotps "u:w:c" opts

	case "$opts"
		u) url="$OPTARG";;
		w) wordlist="$OPTARG";;
		c) filter_status="$OPTARG";;
	esac
done

echo "$url"
echo "$wordlist"
echo "$filter_status" 

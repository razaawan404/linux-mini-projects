#!/usr/bin/env bash


while getopts "u:w:c:" opts
do
	case "$opts" in
		u) url="$OPTARG";;
		w) wordlist="$OPTARG";;
		c) filter_status="$OPTARG";;
	esac
done

echo "$url"
echo "$wordlist"
echo "$filter_status" 
